-- Enable UUID extension
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Create schemas
CREATE SCHEMA IF NOT EXISTS public;

-- Initial setup (actual tables created by TypeORM migrations)
-- This file can be extended with initial data or functions
